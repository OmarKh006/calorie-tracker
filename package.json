{
  "name": "calorie-app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=packages/client\" \"npm run dev --workspace=packages/server\"",
    "build": "npm run build --workspace=packages/client"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "lerna": "^8.0.0"
  }
}
